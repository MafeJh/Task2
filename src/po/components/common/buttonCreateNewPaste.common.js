const BaseComponent = require("./base.component");
class ButtonCreateNewPaste extends BaseComponent {
  constructor(rootSelector) {
    super(rootSelector);
  }
}

module.exports = ButtonCreateNewPaste;
